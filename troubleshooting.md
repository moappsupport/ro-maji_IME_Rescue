---
title: トラブルシューティング
description: ro-maji IME Rescue が動作しないときの原因の切り分け方。症状別の対処法とログの読み方。
---
症状から原因を切り分けるための文書です。設定は、タスクトレイのアイコンをダブルクリックするか、右クリックして「設定を開く」から変更できます。

## 症状別

### 「選択状態を確認できませんでした」 {: #selection-verify-failed}

**誤削除を防ぐために処理を中断した状態です**（`SelectionVerifyFailed`）。文字は削除されていません。

Shift+Left で選択した範囲が、対象の文字列と一致しなかったことを意味する。原因はキー送信の取りこぼし。

上から順に試す。

1. **キー間隔**を増やす（既定 8ms → 15ms 程度）
2. **操作間待機**を増やす（既定 65ms → 100ms）

選択は 1 キーずつ送るため、**キー間隔が最も効く**。文字数が多いほど取りこぼしやすいので、長い文章で失敗する場合はまずここを上げる。

**コピー完了待機は無関係。**ログに `mismatch` と出ていれば読み取り自体は成功しているため、この設定を増やしても改善しない。

ログの `Verify target selection: mismatch expected=101 actual=96` のように、actual が expected より小さい場合は取りこぼしが起きている。

### 元の文字が消えず、その後ろに再入力される {: #leftover-text}

`konnnichihaこんにちは` のようになる症状。選択が短くなり、Backspace で消しきれなかった分が残っている。

原因と対処は [「選択状態を確認できませんでした」](#selection-verify-failed)と同じ。選択の検証をすり抜けてしまった場合に起きる。

### 入力が欠ける {: #missing-characters}

`konnnichiha` が `konichiha` のように一部欠けて再入力される。

**キー間隔**を増やす（既定 8ms → 15ms 程度）。選択と再入力の両方に効く。

### 更新後に「待機時間の設定を既定値に戻しました」と出た {: #wait-settings-reset}

**意図した動作**。v1.0.2 で待機時間の扱いを変更したため、一度だけ既定値へ戻している。

それ以前のバージョンでは、Windows の制約により待機時間が指定どおりに動いていなかった。1〜15ms の指定はすべて約 15.6ms、20ms の指定は約 31ms というように、実際にはより長く待っていた。この状態で調整された設定値は、精度が上がった現在では前提が変わってしまい、そのまま使うと待機が短くなりすぎて取りこぼしの原因になる。

- **戻るのは待機時間の 7 項目のみ**。取得範囲とクリップボードの設定はそのまま引き継がれる
- 戻るのは**一度だけ**。その後に変更した値は保持される
- 以前の設定値が必要な場合は、設定画面から入力し直す。ただし数値の意味が変わっているため、同じ値にしても以前と同じ動作にはならない

キー間隔の既定値は 1ms から 8ms に変わっているが、**実際の動作は以前より速い**（以前の 1ms は実質 15.6ms だったため）。

### 変換候補が出ない／ローマ字のまま残る {: #ime-not-responding}

削除と再入力は動いているが、IME が受け取っていない。

| 対処 | 設定 |
|---|---|
| IME ON 後の待機を増やす | **IME ON待機** 65ms → 100〜150ms |
| 変換モード設定後の待機を増やす | **ひらがなモード変更待機** 65ms → 100〜150ms |
| 変換キー送信前の待機を増やす | **入力後待機** 65ms → 100〜150ms |

それでも直らない場合、対象アプリまたは IME が IMM32 のメッセージに応答していない可能性がある。ログに `ImeSet(1)` と `ImeSetConversionMode(0x19)` は出るが結果が反映されていない状況で、コード側では `VK_IME_ON` キー送信によるフォールバックまで実施している。ここで駄目なら、そのアプリ・IME の組み合わせでは動作しない。

### 「コピーできませんでした」 {: #copy-timeout}

`CopyTimeout`。ログでは `clipboard timeout`。

| 原因 | 対処 |
|---|---|
| コピー完了が待機時間内に終わらない | **コピー完了待機**（既定 0.5 秒）を 1.0 秒程度に増やす |
| 対象アプリが Ctrl+C に対応していない | そのアプリでは動作しない。仕様上の制約 |
| 選択自体が空だった | 取得範囲を変えて試す |

### 「クリップボードを操作できません」 {: #clipboard-error}

`ClipboardError`。ログでは `clipboard poll: transient error`。

クリップボードを他のプロセスがロックしている。よくある原因:

- クリップボード履歴・拡張ツール（Clibor、Ditto など）
- リモートデスクトップのクリップボード同期
- 仮想化ソフト（VMware、VirtualBox）のクリップボード共有

対処:

- **コピー完了待機**を増やす。この待機時間の間はリトライを続けるため、一過性のロックなら乗り越えられる
- 該当ツールを一時的に終了して切り分ける

### 変換キーを押しても何も起きない {: #convert-key-no-response}

ログに `trigger: Convert` が出ない場合。原因は7つ。

| 原因 | 確認方法 | 対処 |
|---|---|---|
| **キーボードに変換キーが無い** | 英語配列（US / ANSI）のキーボードを使っていないか | 変換キーは日本語配列（106/109 キー）専用。本アプリ側に割り当てを変更する機能は無いが、AutoHotkey 等で任意のキーから変換キーを送るようにすれば動作する（送信元のキーは区別しないため）|
| **Windows のキーボードレイアウトが英語配列** | 設定 → 時刻と言語 → 言語と地域 → 日本語 → キーボードレイアウト | 日本語配列のキーボードでもレイアウト設定が英語配列だと変換キーが認識されない。日本語配列に変更して再サインイン |
| **IME が ON だった** | 言語バーが「あ」になっていないか | 仕様。IME OFF（「A」表示）のときだけ動作する。ON のときは通常の変換キーとして素通しする |
| **修飾キーを一緒に押していた** | Shift / Ctrl / Alt / Windows キーを押していないか | 変換キーを単体で押す |
| **IME 状態の問い合わせがタイムアウトした** | 対象アプリが重い・応答が遅い状態でないか | 50ms 以内に IME が OFF と確定できないと安全側に倒して素通しする。アプリの負荷が下がってから再試行する |
| **アプリが起動していない** | タスクトレイにアイコンがあるか | 起動する |
| **フックの登録に失敗した** | 起動時に「キーボード監視の登録に失敗しました」が出ていないか | アプリを再起動する |

`trigger: Convert` は出るが `result: NoAscii` で終わる場合は[「救済対象の半角英数が見つかりませんでした」](#no-ascii-found)を参照。

### 「救済対象の半角英数が見つかりませんでした」 {: #no-ascii-found}

ログでは `RegEx match: False` または `result: NoAscii`。この結果のときだけ通知は出ず、通常の変換キー動作にフォールバックする。

救済対象は **カーソル直前の連続した半角 ASCII 印字文字**。以下は対象外。

- **半角スペースの直後にカーソルがある** — スペースは対象文字に含まれない。`konnnichiha ` のように末尾に空白があると何も拾わない
- **全角文字の直後** — 既に変換済みの日本語は対象外
- **カーソルが行頭にある** — 直前が改行なので読み取る文字がない

対処:

- スペースを消してから変換キーを押す
- 行頭にいる場合は、変換したい文字列の直後へカーソルを移動してから変換キーを押す

**取得範囲を変えても、Enter で改行した前の行は変換できません。** 詳しくは[Enter で改行した前の行が変換されない](#no-multiline)を参照。

### Enter で改行した前の行が変換されない {: #no-multiline}

**Enter キーで改行した位置を越えて変換することはできません。** 変換されるのは、直前の改行からカーソルまでの範囲だけです。

```
kyouhaiitenki          ← Enter で改行したので変換されない
desunodesannposisimasu ← ここにカーソルがあると、この範囲だけ変換される
```

**画面端での折り返しは影響しません。** 長い文章が折り返して複数行に見えていても、Enter を押していなければ 1 行として扱われ、まとめて変換されます。区切りになるのは Enter による改行だけです。

**取得範囲の設定を変えても解決しません。** 取得範囲は読み取る量を決めるだけで、変換対象は常に「カーソル直前の連続した半角英数」です。改行はこの範囲に含まれないため、そこで必ず区切られます。

#### 対処

**改行で区切られた範囲ごとに変換してください。** 各範囲の末尾にカーソルを置いて、順に変換キーを押します。

#### 改行の直後にカーソルがあると何もできない場合があります

アプリによっては、改行の直後にカーソルを置いた状態では前の行を読み取れません。改行コードの扱いがアプリごとに異なるためです（メモ帳などが該当します）。

この場合は、変換したい文字列の直後へカーソルを移動してから変換キーを押してください。

### 残したかった英単語まで変換されてしまう {: #unwanted-conversion}

`apiwotukau` と小文字で打って変換キーを押すと、`api` の部分まで巻き込まれて「アピを使う」になる。

**小文字の英単語はローマ字と区別できない。** `api` が英語の API なのかローマ字の「あぴ」なのかは、文字列だけからは判定する手段がない。意図して入力した英単語と、IME をオフのまま打ってしまったローマ字は同じ見た目になる。

**大文字で入力すれば保持される。** `API` `OK` `URL` `PDF` のように大文字が 2 文字以上続く部分は、そのままの表記で残る。

```
apiwotukau              → アピを使う（小文字は区別できない）
APIwotukau              → APIをつかう
kyouhaHTMLwobenkyou     → きょうはHTMLをべんきょう
```

母音を含む略語ほど影響が大きい。`API` は「アピ」になって原形が失われるが、`CSS` のように母音を含まない語は「css」となり表記が崩れるだけで文字自体は残る。

#### 対処

小文字の英単語を残したい場合は、**半角スペースで区切る。** 空白は対象に含まれない（`\x20` は `\x21-\x7E` の範囲外）ため、そこで切れる。

```
testnokakikata      → test ごと変換されてしまう
test nokakikata     → nokakikata だけが変換される
```

変換後にスペースを削除すれば意図した結果になる。

**打ち間違えに気づいた時点で、すぐ変換キーを押す。** 後続を打ち進めるほど巻き込む範囲が広がる。

#### 実行してしまった場合

変換候補が表示された未確定の状態であれば、Esc で変換を取り消してから Ctrl+Z を数回押すことで、削除前の状態に戻せる場合がある。本アプリは「削除 → 再入力」の順で処理するため、元に戻すには複数回の取り消しが必要になる。

ただし取り消しの挙動は対象アプリに依存するため、必ず戻せるとは限らない。重要な文書では、あらかじめ短い文で動作を確認しておくことを勧める。

### 大文字が小文字になる {: #lowercase-only}

大文字が保持されるのは、**2 文字以上続いている場合**だけ。1 文字だけの大文字は小文字になる。

| 入力 | 結果 | 理由 |
|---|---|---|
| `APIwotukau` | `APIをつかう` | 大文字が 2 文字以上続く |
| `OKdesu` | `OKです` | 同上 |
| `HTML` | `HTML` | 同上（変換されずそのまま残る） |
| `Konnnichiha` | `こんにちは` | 大文字が 1 文字 |
| `desuI` | `ですい` | 同上 |

単独の大文字を小文字にするのは、**文頭を大文字にしただけのローマ字**（`Konnnichiha`）を変換できるようにするため。日本語 IME は大文字が 1 文字だけの場合、その文字以降を半角英数として扱うので、そのまま送ると一切変換されない。

大文字 1 文字を残したい場合は、[「残したかった英単語まで変換されてしまう」](#unwanted-conversion)と同じく、残したい部分との間に半角スペースを入れる。

### クリップボードの内容が消える／戻らない {: #clipboard-not-restored}

処理中に内部で Ctrl+C を使うため、クリップボードは必ず一度上書きされる。処理後の扱いは設定で決まる。

| 設定 | 処理後の状態 |
|---|---|
| そのまま保持する（既定） | 救済対象の文字列が残る |
| 元の内容を復元する | 処理前のテキストが戻る |
| 空にする | 空になる |

**復元してもテキストしか戻らない。** 画像・ファイル・書式付きテキストなどは復元対象外で、その場合は空になる。ログの `clipboard backup: none (empty/non-text)` がこの状態を示す。

`clipboard backup failed:` と出ている場合は退避そのものに失敗している。原因は[「クリップボードを操作できません」](#clipboard-error)と同じ。

### 「対象ウィンドウを前面にできませんでした」 {: #focus-failed}

`FocusFailed`。対象アプリが管理者権限で動いている。

Windows の UIPI（User Interface Privilege Isolation）により、権限の低いプロセスから高いプロセスのウィンドウへ入力を送れない。これは OS のセキュリティ境界のため、アプリ側の設定では回避できない。

対処: 対象アプリと同じく、本アプリも管理者権限で起動し直す。

### 「コピー結果がテキストではありません」 {: #copy-non-text}

`CopyNonText`。選択範囲に画像などテキスト以外が含まれているか、対象アプリが独自形式でクリップボードに書き込んでいる。そのアプリでは動作しない。

### 救済処理中に一瞬キーボードが効かなくなる {: #keyboard-blocked-briefly}

**仕様**。処理中は物理キーの入力を握り潰している。ユーザーの Ctrl や Shift が合成 Backspace と組み合わさって Ctrl+Backspace（単語削除）などに化けるのを防ぐため。

握り潰しは、処理が 3 秒間まったく進まなくなった場合に自動解除される（処理が詰まってもキーボードが無反応にならないための安全弁）。基準は開始からの経過時間ではなく最後に処理が進んだ時点からの経過時間なので、長い文字列を扱っていて全体で数秒かかる場合でも、処理が進んでいる限り解除されない。念のため総時間 30 秒の上限も併用している。

待機時間の設定を増やしても、この解除条件に影響しない。

### 設定が反映されない {: #settings-not-applied}

- 設定ファイルは `%LOCALAPPDATA%\ro-maji_ime_rescue\ro-maji_ime_rescue.ini`
- 有効範囲外の値は読み込み時・保存時の両方で自動的に丸められる（例: 操作間待機の最小値は 15ms）
- 設定画面で OK を押した時点で反映される。アプリの再起動は不要

### 自動起動を有効にできない {: #startup-not-enabled}

設定画面のチェックボックスが操作できない、または有効にしても反映されない場合。管理者権限は関係ない。

| 状態 | 原因 | 対処 |
|---|---|---|
| チェックできない・「タスク マネージャーで無効にされています」と表示 | タスク マネージャーの「スタートアップ アプリ」で無効にされている | **アプリ側からは変更できない仕様**。タスク マネージャーを開き、「スタートアップ アプリ」タブで ro-maji IME Rescue を選んで「有効にする」 |
| 「組織のポリシーによって禁止されています」と表示 | グループポリシーで自動起動が禁止されている | 管理者に問い合わせる |
| 「インストールされたアプリとして実行されていない」と表示 | パッケージからインストールしたものではなく、ビルド出力の exe を直接実行している | インストーラー経由で入れ直す |

一度タスク マネージャーで無効にすると、アプリ側から再度有効にすることはできない。これは Windows の仕様で、ユーザーの明示的な意思をアプリが上書きできないようにするためのもの。

### アンチウイルス／EDR が警告を出す {: #antivirus-warning}

システム全体を監視する低レベルキーボードフック（`WH_KEYBOARD_LL`）を使っているため、キーロガーと同じ API として検知されることがある。

このフックが見ているのは変換キーの押下・離上のタイミングだけで、他のキー入力の内容は記録も送信もしていない。デバッグログにも文字数と成否しか書かない。

より制限された `RegisterHotKey` を使わないのは、「IME が ON のときは素通しする」という分岐が実現できないため。

### 「すでに起動しています」と出る {: #already-running}

多重起動を防いでいる。タスクトレイに既にアイコンがあるはず。見当たらない場合はタスクマネージャーで `ro-maji_ime_rescue.exe` を終了してから起動し直す。

---

## 設定値の調整早見表

| 症状 | 増やす設定 |
|---|---|
| 選択・削除が追いつかない | 操作間待機 |
| コピーに失敗する | コピー完了待機 |
| 選択がずれる | キー間隔 |
| 再入力の文字が欠ける | キー間隔 |
| 変換候補が出ない | IME ON待機、ひらがなモード変更待機、入力後待機 |

キー間隔は「1キーごとの間隔」で、**選択と再入力の両方に適用される**。文字数に比例して効くため、処理全体の速度を最も左右する設定でもある。

まず**操作間待機**だけを調整して様子を見る。ラグを感じる場合は逆に下げる（最小 15ms）。

クリップボードの復元モードは待機時間と無関係。復元処理は救済のキー操作が始まる前と全部終わった後にまとめて行われるため、これらの設定を変える必要はない。

## 動作しないケース

以下は設定では解決できない。

- **キーボードに変換キーが無い**（英語配列 / US・ANSI）。トリガーは変換キーのみで、本アプリ側に割り当てを変更する機能は無い。外部のキー割り当てツールで変換キーを送れるようにすれば動作する
- **Enter による改行をまたいだ変換**。取得範囲を変えても前の行は拾えない。画面端での折り返しは影響しない（[詳細](#no-multiline)）
- 対象アプリが Ctrl+C・Shift+Home などの標準的なキー操作に対応していない
- 対象アプリが管理者権限で動いている（本アプリも同じ権限で起動すれば動く）
- パスワード欄など、内容を読み取れない入力欄
- IME が IMM32 のメッセージにも `VK_IME_ON` にも応答しない

## デバッグモードで詳しく調べる

原因の特定はログを見るのが最短。

1. トレイアイコンを右クリック →「デバッグモードで再起動」
2. 問題が起きる操作をもう一度する
3. トレイメニュー →「デバッグログを開く」

ログは `%LOCALAPPDATA%\ro-maji_ime_rescue\ro-maji_ime_rescue_debug.txt`。コピーした本文やクリップボードの中身は記録されず、文字数と成否だけが残る。

### ログの読み方

正常に完走した場合の例。

```
trigger: Convert hwnd=656710
clipboard backup: len=20
=== ReType start ===
Select range
GetClipboardText start
Ctrl+C sent
clipboard got: len=11
RegEx match: True len=11
Select target: Shift+Left x11
Verify target selection: ok len=11
SendBackspace
ImeSet(1)
ImeSetConversionMode(0x19)
TypeAsKeys start
SendConvert
=== ReType end: true ===
clipboard restored: len=20
result: Success
```

**どこまで進んだか**でどこが失敗したかが分かる。以下の各行が出ない場合が切り分けポイント。

| 出ない行 | 意味 |
|---|---|
| `trigger: Convert` | 変換キーの検知自体に至っていない → [変換キーを押しても何も起きない](#convert-key-no-response) |
| `clipboard got` | 選択範囲のコピーに失敗 → [「コピーできませんでした」](#copy-timeout) |
| `RegEx match: True` | 救済対象の半角英数が見つからない → [「救済対象の半角英数が見つかりませんでした」](#no-ascii-found) |
| `Verify target selection: ok` | 選択が想定とずれている → [「選択状態を確認できませんでした」](#selection-verify-failed) |
| `=== ReType end: true ===` | 再入力・変換の段階で例外 → [変換候補が出ない／ローマ字のまま残る](#ime-not-responding) |

---

[トップページへ戻る](./)

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "選択状態を確認できませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "誤削除を防ぐために処理を中断した状態です（SelectionVerifyFailed）。文字は削除されていません。"
      }
    },
    {
      "@type": "Question",
      "name": "元の文字が消えず、その後ろに再入力される",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "konnnichihaこんにちは のようになる症状。選択が短くなり、Backspace で消しきれなかった分が残っている。"
      }
    },
    {
      "@type": "Question",
      "name": "入力が欠ける",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "konnnichiha が konichiha のように一部欠けて再入力される。"
      }
    },
    {
      "@type": "Question",
      "name": "更新後に待機時間の設定を既定値に戻しましたと出た",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "意図した動作。v1.0.2 で待機時間の扱いを変更したため、一度だけ既定値へ戻している。"
      }
    },
    {
      "@type": "Question",
      "name": "変換候補が出ない／ローマ字のまま残る",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "削除と再入力は動いているが、IME が受け取っていない。"
      }
    },
    {
      "@type": "Question",
      "name": "コピーできませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "CopyTimeout。ログでは clipboard timeout。"
      }
    },
    {
      "@type": "Question",
      "name": "クリップボードを操作できません",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "ClipboardError。ログでは clipboard poll: transient error。"
      }
    },
    {
      "@type": "Question",
      "name": "変換キーを押しても何も起きない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "ログに trigger: Convert が出ない場合。原因は7つ。"
      }
    },
    {
      "@type": "Question",
      "name": "救済対象の半角英数が見つかりませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "ログでは RegEx match: False または result: NoAscii。この結果のときだけ通知は出ず、通常の変換キー動作にフォールバックする。"
      }
    },
    {
      "@type": "Question",
      "name": "Enter で改行した前の行が変換されない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Enter キーで改行した位置を越えて変換することはできません。 変換されるのは、直前の改行からカーソルまでの範囲だけです。"
      }
    },
    {
      "@type": "Question",
      "name": "残したかった英単語まで変換されてしまう",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "apiwotukau と小文字で打って変換キーを押すと、api の部分まで巻き込まれてアピを使うになる。"
      }
    },
    {
      "@type": "Question",
      "name": "大文字が小文字になる",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "大文字が保持されるのは、2 文字以上続いている場合だけ。1 文字だけの大文字は小文字になる。"
      }
    },
    {
      "@type": "Question",
      "name": "クリップボードの内容が消える／戻らない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "処理中に内部で Ctrl+C を使うため、クリップボードは必ず一度上書きされる。処理後の扱いは設定で決まる。"
      }
    },
    {
      "@type": "Question",
      "name": "対象ウィンドウを前面にできませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "FocusFailed。対象アプリが管理者権限で動いている。"
      }
    },
    {
      "@type": "Question",
      "name": "コピー結果がテキストではありません",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "CopyNonText。選択範囲に画像などテキスト以外が含まれているか、対象アプリが独自形式でクリップボードに書き込んでいる。そのアプリでは動作しない。"
      }
    },
    {
      "@type": "Question",
      "name": "救済処理中に一瞬キーボードが効かなくなる",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "仕様。処理中は物理キーの入力を握り潰している。ユーザーの Ctrl や Shift が合成 Backspace と組み合わさって Ctrl+Backspace（単語削除）などに化けるのを防ぐため。"
      }
    },
    {
      "@type": "Question",
      "name": "設定が反映されない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "設定ファイルは %LOCALAPPDATA%\\ro-maji_ime_rescue\\ro-maji_ime_rescue.ini 有効範囲外の値は読み込み時・保存時の両方で自動的に丸められる（例: 操作間待機の最小値は 15ms） 設定画面で OK を押した時点で反映される。アプリの再起動は不要"
      }
    },
    {
      "@type": "Question",
      "name": "自動起動を有効にできない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "設定画面のチェックボックスが操作できない、または有効にしても反映されない場合。管理者権限は関係ない。"
      }
    },
    {
      "@type": "Question",
      "name": "アンチウイルス／EDR が警告を出す",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "システム全体を監視する低レベルキーボードフック（WH_KEYBOARD_LL）を使っているため、キーロガーと同じ API として検知されることがある。"
      }
    },
    {
      "@type": "Question",
      "name": "すでに起動していますと出る",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "多重起動を防いでいる。タスクトレイに既にアイコンがあるはず。見当たらない場合はタスクマネージャーで ro-maji_ime_rescue.exe を終了してから起動し直す。"
      }
    }
  ]
}
</script>
