---
title: トラブルシューティング
description: ro-maji IME Rescue が動作しないときの原因の切り分け方。症状別の対処法とログの読み方。
updated: 2026-08-10
---
症状から原因を切り分けるための文書です。設定は、タスクトレイのアイコンをダブルクリックするか、右クリックして「設定を開く」から変更できます。

## 設定値の調整早見表

**まず設定画面で「安定重視」を選んでください。** 待機値がまとめて増える分、成功率が上がります。処理は遅くなるので、それでも解決しない場合だけ、以下を参考に詳細設定で個別に調整してください。

**読み取り上限・選択確認の上限・キーボード保護の上限はプリセットに含まれないため、「安定重視」にしても変わりません。** これらは待機時間ではなく「諦めるまでの上限」なので、必要な場合は詳細設定で個別に増やしてください。

| 症状 | 増やす設定 |
|---|---|
| 選択・削除が追いつかない | 操作間待機 |
| 変換対象が見つからないことが多い | 読み取り上限 |
| 選択がずれる | キー間隔 |
| 長い文章で「選択状態を確認できませんでした」が出る | 選択確認の上限 |
| 再入力の文字が欠ける | キー間隔 |
| 変換候補が出ない | IME ON待機、ひらがなモード変更待機、入力後待機 |
| 長い文章の途中でキーボードが効くようになる | キーボード保護の上限 |

キー間隔は 1 キーごとの間隔で、選択と再入力の両方に適用されます。文字数に比例して効くため、処理全体の速度を最も左右する設定でもあります。

ラグを感じる場合は「最速」を選んでください。詳細設定でもこれ以上は下げられません（キー間隔の最小 4ms、他は最小 20ms）。

クリップボードの復元モードは待機時間と無関係です。復元処理はキー操作の前後にまとめて行われるため、これらの設定を変える必要はありません。

**選択の取りこぼしは自動でも1回だけ再試行されます。** 詳しくは[「選択状態を確認できませんでした」](#selection-verify-failed)を参照してください。

## 症状別

<nav class="toc">
  <p class="toc-title">症状から探す</p>
  <ol>
    <li><a href="#selection-verify-failed">「選択状態を確認できませんでした」</a></li>
    <li><a href="#leftover-text">元の文字が消えず、その後ろに再入力される</a></li>
    <li><a href="#missing-characters">入力が欠ける</a></li>
    <li><a href="#wait-settings-reset">更新後に「待機時間の設定を既定値に戻しました」と出た</a></li>
    <li><a href="#ime-not-responding">変換候補が出ない／ローマ字のまま残る</a></li>
    <li><a href="#clipboard-error">「クリップボードを操作できません」</a></li>
    <li><a href="#convert-key-no-response">キーを押しても何も起きない</a></li>
    <li><a href="#no-ascii-found">「救済対象の半角英数が見つかりませんでした」</a></li>
    <li><a href="#no-multiline">Enter で改行した前の行が変換されない</a></li>
    <li><a href="#long-text">長い文章を一度に変換すると、先頭が勝手に確定される</a></li>
    <li><a href="#unwanted-conversion">残したかった英単語まで変換されてしまう</a></li>
    <li><a href="#clipboard-not-restored">クリップボードの内容が消える／戻らない</a></li>
    <li><a href="#interrupted">「処理が中断されました」</a></li>
    <li><a href="#focus-failed">「対象ウィンドウを前面にできませんでした」</a></li>
    <li><a href="#copy-non-text">「コピー結果がテキストではありません」</a></li>
    <li><a href="#unknown-failed">「救済できませんでした」</a></li>
    <li><a href="#keyboard-blocked-briefly">救済処理中に一瞬キーボードが効かなくなる</a></li>
    <li><a href="#settings-not-applied">設定が反映されない</a></li>
    <li><a href="#startup-not-enabled">自動起動を有効にできない</a></li>
    <li><a href="#antivirus-warning">アンチウイルス／EDR が警告を出す</a></li>
    <li><a href="#already-running">「すでに起動しています」と出る</a></li>
  </ol>
</nav>

### 「選択状態を確認できませんでした」 {: #selection-verify-failed}

誤削除を防ぐために処理を中断した状態です。文字は削除されていません。原因はログの内容によって2通りに分かれます。

**ログに `mismatch` と出ている場合**（Shift+Left で選択した範囲が対象の文字列と一致しなかった）

原因はキー送信の取りこぼしです。**このメッセージが出る時点で、既に1段安全な設定で自動的に1回だけ再試行されています。** それでも解決しなかった場合に表示されます。

1. **設定を「安定重視」にする**（詳細設定のキー間隔・操作間待機がまとめて増えます）
2. それでも直らない場合は、詳細設定で**キー間隔**をさらに増やしてください

選択は 1 キーずつ送るため、キー間隔が最も効きます。文字数が多いほど取りこぼしやすいので、長い文章で失敗する場合はここが特に効きます。読み取り上限は無関係です。読み取り自体は成功しているため、増やしても改善しません。

**すでにキー間隔をかなり増やした状態でも取りこぼす場合、自動再試行は行われません。** そこまで遅くしても解決しないなら、原因は速度ではなく対象のアプリ側にある可能性が高いためです。この場合は「設定を変えても解決しない場合は、対象のアプリ側に原因がある可能性があります」という文言が表示されます。

**ログに `mismatch` が出ておらず読み取り自体に失敗している場合**（クリップボードの内容を確認できなかった）

選択内容の確認は、**「選択確認の上限」（既定 5 秒）**まで待ちます。読み取れた時点ですぐ次へ進むため、通常はこの上限に届きません（実測では平均 40ms、最も遅い例でも 149ms でした）。上限まで待っても失敗するのは、クリップボードを他のアプリが使用しているか、対象のアプリが Ctrl+C 自体を処理していない場合です。安全のため自動再試行は行われません。

クリップボード履歴ツールやリモートデスクトップを一時的に終了して切り分けてください。詳しくは[「クリップボードを操作できません」](#clipboard-error)を参照してください。

**2000 文字を超えるような長い文章や、動作の重いアプリでは 5 秒でも足りないことがあります。** その場合は詳細設定で「選択確認の上限」を増やしてください。

---

同一起動中に `mismatch` の取りこぼしが3回目に達すると、失敗の通知とは別に「取りこぼしが続いています」という案内が1回だけ表示されます。

### 元の文字が消えず、その後ろに再入力される {: #leftover-text}

`konnnichihaこんにちは` のようになる症状です。選択が短くなり、Backspace で消しきれなかった分が残っています。

原因と対処は[「選択状態を確認できませんでした」](#selection-verify-failed)と同じです。選択の検証をすり抜けた場合に起きます。

### 入力が欠ける {: #missing-characters}

`konnnichiha` が `konichiha` のように一部欠けて再入力される症状です。

設定を「安定重視」にするか、詳細設定で**キー間隔**を増やしてください。選択と再入力の両方に効きます。

### 更新後に「待機時間の設定を既定値に戻しました」と出た {: #wait-settings-reset}

意図した動作です。古いバージョンから更新した際、待機時間の仕組みが変わったため一度だけ既定値へ戻しています。

- 戻るのは**待機時間の項目のみ**です。取得範囲とクリップボードの設定は引き継がれます
- 戻るのは**一度だけ**です。その後に変更した値は保持されます
- 以前の値に戻したい場合は設定画面から入力し直してください

### 変換候補が出ない／ローマ字のまま残る {: #ime-not-responding}

削除と再入力は動いているものの、IME が受け取っていない状態です。

設定を「安定重視」にすると、以下の項目がまとめて増えます。改善しない場合は詳細設定で個別に増やしてください。

| 対処 | 設定 |
|---|---|
| IME ON 後の待機を増やす | **IME ON待機** 40ms → 60〜100ms |
| 変換モード設定後の待機を増やす | **ひらがなモード変更待機** 40ms → 60〜100ms |
| 変換キー送信前の待機を増やす | **入力後待機** 40ms → 60〜100ms |

それでも直らない場合、対象アプリまたは IME が IMM32 のメッセージに応答していない可能性があります。コード側では `VK_IME_ON` キー送信によるフォールバックまで実施しているため、ここで駄目ならその組み合わせでは動作しません。

### 「クリップボードを操作できません」 {: #clipboard-error}

クリップボードを他のプロセスがロックしています。よくある原因は次のとおりです。

- クリップボード履歴・拡張ツール（Clibor、Ditto など）
- リモートデスクトップのクリップボード同期
- 仮想化ソフト（VMware、VirtualBox）のクリップボード共有

**ロックを解除する手段は本アプリ側にありません。** クリップボードは Windows 全体で 1 つしかない共有資源で、開いたプロセスだけが閉じられるためです。上記のようなツールを一時的に終了して切り分けてください。

ごく短時間のロック（コピー直後の一瞬など）は Windows 側の再試行で自動的に乗り越えられるため、この通知は出ません。この通知が出るのは約 1 秒待っても解放されなかった場合で、さらに待っても解決する見込みが薄いため、その時点で中断しています。

### キーを押しても何も起きない {: #convert-key-no-response}

起動用のキーは設定画面で**変換キー**または**右Ctrl キー**から選べます。ログに `trigger: Convert`（または `trigger: RightCtrl`）が出ない場合、原因は次の 8 つです。

| 原因 | 確認方法 | 対処 |
|---|---|---|
| **キーボードに変換キーが無い** | 英語配列（US / ANSI）のキーボードを使っていないか | 変換キーは日本語配列（106/109 キー）専用です。設定画面で起動用のキーを**右Ctrl キー**に変更してください |
| **Windows のキーボードレイアウトが英語配列** | 設定 → 時刻と言語 → 言語と地域 → 日本語 → キーボードレイアウト | 日本語配列のキーボードでもレイアウト設定が英語配列だと変換キーが認識されません。変更して再サインインするか、右Ctrl キーに切り替えてください |
| **IME が ON だった** | 言語バーが「あ」になっていないか | 仕様です。IME OFF（「A」表示）のときだけ動作します |
| **修飾キーを一緒に押していた** | Shift / Ctrl / Alt / Windows キーを押していないか | 起動用のキーを単体で押してください（右Ctrl キーを選んでいる場合、除外されるのは**左**Ctrl です） |
| **右Ctrl キーを長く押していた** | ログに `trigger skipped: held=...` が出ていないか | 右Ctrl キーは**単独で短く押したとき**だけ働きます。詳しくは下記を参照してください |
| **IME 状態の問い合わせがタイムアウトした** | 対象アプリが重い状態でないか | 50ms 以内に IME が OFF と確定できないと安全側に倒して素通しします。負荷が下がってから再試行してください |
| **アプリが起動していない** | タスクトレイにアイコンがあるか | 起動してください |
| **フックの登録に失敗した** | 起動時に「キーボード監視の登録に失敗しました」が出ていないか | アプリを再起動してください |

**右Ctrl キーを選んだ場合、押しっぱなしにすると働きません。** Ctrl+クリック（リンクを新しいタブで開く等）や Ctrl+ホイール（ズーム）を右Ctrl キーで行ったときに、指を離した瞬間に救済処理が走ってしまうのを防ぐためです。マウス操作はキー操作と違ってアプリ側から検知できないため、押している時間の長さで区別しています。Ctrl+C のようなキーとの組み合わせは長さに関係なく除外されます。

`trigger:` は出るが `result: NoAscii` で終わる場合は[「救済対象の半角英数が見つかりませんでした」](#no-ascii-found)を参照してください。

### 「救済対象の半角英数が見つかりませんでした」 {: #no-ascii-found}

救済対象は**カーソル直前の連続した半角 ASCII 印字文字**です。次の場合は対象がありません。

- **半角スペースの直後にカーソルがある** — スペースは対象文字に含まれません
- **全角文字の直後** — 既に変換済みの日本語は対象外です
- **カーソルが行頭にある** — 直前が改行なので読み取る文字がありません

スペースを消すか、変換したい文字列の直後へカーソルを移動してから起動用のキー（変換キーまたは右Ctrl キー）を押してください。

取得範囲を変えても、Enter で改行した前の行は変換できません。詳しくは[Enter で改行した前の行が変換されない](#no-multiline)を参照してください。

### Enter で改行した前の行が変換されない {: #no-multiline}

**Enter キーで改行した位置を越えて変換することはできません。** 変換されるのは、直前の改行からカーソルまでの範囲だけです（取得範囲の設定を変えても解決しません）。

```
kyouhaiitenki          ← Enter で改行したので変換されない
desunodesannposisimasu ← ここにカーソルがあると、この範囲だけ変換される
```

画面端での折り返しは影響しません。改行で区切られた範囲ごとに、末尾へカーソルを置いて順に起動用のキーを押してください。

なお、アプリによっては改行の直後にカーソルを置いた状態で前の行を読み取れません（メモ帳などが該当します）。この場合も、変換したい文字列の直後へカーソルを移動してください。

### 長い文章を一度に変換すると、先頭が勝手に確定される {: #long-text}

**一度に変換する文字数が多いと、IME が先頭のほうから順に変換を自動で確定していきます。** 確定された部分は変換候補を選び直せなくなるため、誤変換の修正に手間がかかります。

目安は**ローマ字で 150 文字程度**です。実測では 150 文字までは確定されず、160 文字を超えたあたりから先頭が確定され始めました。ただし文の区切り方によって前後します（同程度の長さでも、確定が始まる位置が 2 倍近く変わる例がありました）。500 文字では 6〜7 割が、2500 文字ではほとんどが確定済みになります。

処理時間も文字数に比例して伸びます。**処理中はキーを打たず、他のウィンドウもクリックしないでください。** どちらも処理の中断につながります（[「処理が中断されました」](#interrupted)）。とくにクリックはアプリ側で止められないため、処理が長いほど当たりやすくなります。

**長い文章ほどこの現象が起きやすくなります。** 文や文節の区切りごとに変換すると、変換候補を選び直せる状態を保てます。

### 残したかった英単語まで変換されてしまう {: #unwanted-conversion}

`apiwotukau` と小文字で打つと、`api` の部分まで巻き込まれて「アピを使う」になります。**小文字の英単語はローマ字と区別する方法がないためです。**

```
apiwotukau              → アピを使う（小文字は区別できない）
APIwotukau              → APIを使う
Bandeikimasu            → B案で行きます
```

**大文字で入力すれば保持されます。** `API` のような複数文字はもちろん、「A社」「B案」のような1文字だけの大文字も保持されます。残したい場合は、間に半角スペースを入れる方法もあります（空白は対象に含まれないため、そこで切れます）。

`export` のようにローマ字として成立しない綴りは、`PDFwoexportshimasu → PDFを得x歩rtします` のように、かなと英字が混じった結果になります。

変換候補が表示された未確定の状態なら、Esc で取り消してから Ctrl+Z を数回押すと戻せることがあります（本アプリは削除と再入力の順で処理するため、複数回の取り消しが必要です）。ただし対象アプリの挙動に依存するため、必ず戻せるとは限りません。

**クリップボードの扱いが既定の「そのまま保持」であれば、変換前のローマ字がクリップボードに残っています。** 誤変換された日本語部分を選択して Ctrl+V で貼り付ければ、元のローマ字に戻せます。クリップボードの扱いを「元の内容を復元」や「空にする」に変更している場合は残らないため、この方法は使えません（[設定ごとの違い](#interrupted)）。

### クリップボードの内容が消える／戻らない {: #clipboard-not-restored}

処理中に内部で Ctrl+C を使うため、クリップボードは必ず一度上書きされます。処理後の扱いは設定で決まります。

| 設定 | 処理後の状態 | 元のローマ字に戻せるか |
|---|---|---|
| そのまま保持する（既定） | 救済対象の文字列が残る | **Ctrl+V で戻せる** |
| 元の内容を復元する | 処理前のテキストが戻る | 戻せない |
| 空にする | 空になる | 戻せない |

**右の列がこの設定を選ぶ基準です。** 既定の「そのまま保持」でローマ字を残しているのは意図的なもので、誤変換したときや処理が中断されたときに元へ戻すための唯一の手段になっています。処理前のクリップボードを守ることを優先する場合は「元の内容を復元」を選んでください。そのぶん、失敗しても元のローマ字は取り戻せなくなります。

**復元できるのは主要な形式に限られます。** テキスト・書式付きテキスト（Web ページからのコピーなど）・ファイル・画像が対象です。これら以外の形式（アプリ独自の形式や、貼り付ける瞬間に中身を作る形式）は復元されず、その場合は空になります。

ログに `clipboard backup failed:` と出ている場合は退避そのものに失敗しています。原因は[「クリップボードを操作できません」](#clipboard-error)と同じです。

### 「処理が中断されました」 {: #interrupted}

処理中に別のウィンドウへ切り替わったか、キーボード保護の上限を超えた後にキーを押したため、途中で安全に打ち切りました。無関係なウィンドウへ文字が送り込まれるのを防ぐための動作です。文字が消えているかどうかは、通知の2行目で分かります。

**「文字は消えていません」と出た場合**は、削除の前に止まっています。カーソルを戻してもう一度お試しください。ただし対象のアプリに選択範囲が残っていることがあります。この状態で1文字打つと選択ごと消えるので、先に矢印キーを押すかクリックして選択を外してください。

**「元の文字は削除済みです」と出た場合**は、削除した後・打ち直しの途中で止まっています。元のローマ字を戻せるかどうかは、クリップボードの扱いの設定によって変わります。

| クリップボードの扱い | 中断後の状態 | 元のローマ字を戻せるか |
|---|---|---|
| そのまま保持する（既定） | 処理でコピーしたローマ字が残る | **`Ctrl+V` で戻せる** |
| 元の内容を復元する | 処理前の内容に戻される | 戻せない |
| 空にする | 空になる | 戻せない |

#### 中断されないために

**処理中は他のウィンドウやボタンをクリックしないでください。** 本アプリが止められるのはキーボードだけで、**マウス操作は止められません**（使用しているキーボードフックにはマウスの操作が届かないためです）。クリックで別のウィンドウへ移ると、そこから先の入力がそちらへ流れ込むため、処理を中断します。

**長い文章ほど処理時間が伸びます。** 文字数に比例して選択と打ち直しの時間が増えるので、その分クリックしてしまう余地も広がります。文や文節の区切りごとに変換すると、1回あたりの処理が短く済みます。

**キーボード保護の上限を超えると、キーが効くようになります。** 既定は 30 秒で、通常の長さの文章では届きません。超えた後にキーを押すと、その時点で処理を中止します（打った文字が変換中の文章に混ざるのを防ぐためです）。押したキーは対象のアプリに入ります。

### 「対象ウィンドウを前面にできませんでした」 {: #focus-failed}

対象アプリが管理者権限で動いています。

Windows の UIPI により、権限の低いプロセスから高いプロセスのウィンドウへ入力を送れません。OS のセキュリティ境界のため、アプリ側の設定では回避できません。

対象アプリと同じく、本アプリも管理者権限で起動し直してください。

### 「コピー結果がテキストではありません」 {: #copy-non-text}

選択範囲に画像などテキスト以外が含まれているか、対象アプリが独自形式でクリップボードに書き込んでいます。そのアプリでは動作しません。

### 「救済できませんでした」 {: #unknown-failed}

想定していない失敗です。他のメッセージに当てはまらなかった場合に表示されます。

**まず画面を確認してください。** この通知が出るのは処理の途中で予期しない問題が起きた場合で、文字が消えたままになっている可能性があります。元のローマ字はクリップボードに残っていることが多いので、`Ctrl+V` で貼り付けて戻せます（設定のクリップボードの扱いが「そのまま保持」の場合。詳しくは[「処理が中断されました」](#interrupted)の表を参照してください）。

繰り返し出る場合:

1. 設定を**「安定重視」**にする
2. それでも直らない場合は、この文書の末尾「デバッグモードで詳しく調べる」の手順でログを取り、moappsupport@gmail.com までログの内容をお知らせください

### 救済処理中に一瞬キーボードが効かなくなる {: #keyboard-blocked-briefly}

仕様です。処理中は物理キーの入力を握り潰しています。ユーザーの Ctrl や Shift が合成 Backspace と組み合わさり、Ctrl+Backspace（単語削除）などに化けるのを防ぐためです。

**止められるのはキーボードだけで、マウスは止められません。** 使用しているキーボードフックにはマウスの操作が届かないためです。処理中にクリックして別のウィンドウへ移ると、そこから先の入力がそちらへ流れ込むため、処理を中断します（[「処理が中断されました」](#interrupted)）。

握り潰しが解除される条件は2つあります。

- **処理が 4 秒間まったく進まなくなった場合**（想定外の停止に備えた安全装置）。基準は最後に処理が進んだ時点からの経過時間なので、長い文字列で全体に数秒かかっても、処理が進んでいる限り解除されません
- **「キーボード保護の上限」を超えた場合**（既定 30 秒、詳細設定で 5〜60 秒に変更できます）。長い文章で処理が伸びても、キーボードがこれ以上の時間ふさがれることはありません

**上限を超えて解除された後にキーを押すと、その時点で処理を中止します。** 打った文字が変換中の文章に混ざるのを防ぐためです。押したキーは対象のアプリに入ります。

### 設定が反映されない {: #settings-not-applied}

- 設定ファイルは `%LOCALAPPDATA%\ro-maji_ime_rescue\ro-maji_ime_rescue.ini` です
- 有効範囲外の値は読み込み時・保存時の両方で自動的に丸められます（例: 操作間待機の最小値は 20ms）
- 設定画面で OK を押した時点で反映されます。アプリの再起動は不要です

### 自動起動を有効にできない {: #startup-not-enabled}

管理者権限は関係ありません。

| 状態 | 原因 | 対処 |
|---|---|---|
| チェックできない・「タスク マネージャーで無効にされています」と表示 | タスク マネージャーの「スタートアップ アプリ」で無効にされている | **アプリ側からは変更できない仕様です。**タスク マネージャーの「スタートアップ アプリ」タブで ro-maji IME Rescue を選び、「有効にする」を実行してください |
| 「組織のポリシーによって禁止されています」と表示 | グループポリシーで自動起動が禁止されている | 管理者に問い合わせてください |
| 「インストールされたアプリとして実行されていない」と表示 | ビルド出力の exe を直接実行している | インストーラー経由で入れ直してください |

一度タスク マネージャーで無効にすると、アプリ側から再度有効にすることはできません。ユーザーの明示的な意思をアプリが上書きできないようにする Windows の仕様です。

### アンチウイルス／EDR が警告を出す {: #antivirus-warning}

システム全体を監視する低レベルキーボードフック（`WH_KEYBOARD_LL`）を使っているため、キーロガーと同じ API として検知されることがあります。

このフックが見ているのは起動用のキー（変換キーまたは右Ctrl キー）の押下・離上のタイミングだけで、他のキー入力の内容は記録も送信もしていません。デバッグログにも文字数と成否しか書きません。

より制限された `RegisterHotKey` を使わないのは、IME が ON のときは素通しするという分岐を実現できないためです。

### 「すでに起動しています」と出る {: #already-running}

多重起動を防いでいます。タスクトレイに既にアイコンがあるはずです。見当たらない場合はタスクマネージャーで `ro-maji_ime_rescue.exe` を終了してから起動し直してください。

## 動作しないケース

以下は設定では解決できません。

- **変換キーも右Ctrl キーも無いキーボード**。設定画面で選べるのはこの2つだけで、他のキーへ割り当てを変更する機能はありません（英語配列 / US・ANSI のキーボードには変換キーがありませんが、右Ctrl キーで動作します）
- **Enter による改行をまたいだ変換**。取得範囲を変えても前の行は拾えません（[詳細](#no-multiline)）
- **処理中のマウス操作**。キーボードは一時的に止められますが、マウスは止められません。クリックで別のウィンドウへ移ると処理を中断します（[詳細](#interrupted)）
- 対象アプリが Ctrl+C・Shift+Home などの標準的なキー操作に対応していない
- 対象アプリが管理者権限で動いている（本アプリも同じ権限で起動すれば動きます）
- パスワード欄など、内容を読み取れない入力欄
- IME が IMM32 のメッセージにも `VK_IME_ON` にも応答しない

## デバッグモードで詳しく調べる

上の対処で解決しない場合は、ログを見るのが最短です。コピーした本文やクリップボードの中身は記録されず、文字数と成否だけが残ります。

1. トレイアイコンを右クリック →「デバッグモードで再起動」
2. 問題が起きる操作をもう一度する
3. トレイメニュー →「デバッグログを開く」

### ログの読み方

正常に完走した場合の例です。

```
trigger: Convert hwnd=656710
clipboard backup: len=20
=== ReType start ===
Select range
GetClipboardText start
Ctrl+C sent
clipboard got: len=11
RegEx match: True len=11
Select target: Shift+Left x11
Verify target selection: ok len=11
SendBackspace
ImeSet(1)
ImeSetConversionMode(0x19)
TypeAsKeys start
SendConvert
=== ReType end: true ===
clipboard restored: len=20
result: Success
```

どこまで進んだかで、どこが失敗したかが分かります。

| 出ない行 | 意味 |
|---|---|
| `trigger: Convert` / `trigger: RightCtrl` | 起動用のキーの検知自体に至っていない → [キーを押しても何も起きない](#convert-key-no-response) |
| `clipboard got` | 選択範囲のコピーに失敗 → [「クリップボードを操作できません」](#clipboard-error) |
| `RegEx match: True` | 救済対象の半角英数が見つからない → [「救済対象の半角英数が見つかりませんでした」](#no-ascii-found) |
| `Verify target selection: ok` | 選択が想定とずれている → [「選択状態を確認できませんでした」](#selection-verify-failed) |
| `=== ReType end: true ===` | 再入力・変換の段階で例外 → [変換候補が出ない／ローマ字のまま残る](#ime-not-responding) |

---

[トップページへ戻る](./)

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "選択状態を確認できませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "誤削除を防ぐために処理を中断した状態です。文字は削除されていません。原因はログの内容によって2通りに分かれます。"
      }
    },
    {
      "@type": "Question",
      "name": "元の文字が消えず、その後ろに再入力される",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "konnnichihaこんにちは のようになる症状です。選択が短くなり、Backspace で消しきれなかった分が残っています。"
      }
    },
    {
      "@type": "Question",
      "name": "入力が欠ける",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "konnnichiha が konichiha のように一部欠けて再入力される症状です。"
      }
    },
    {
      "@type": "Question",
      "name": "更新後に待機時間の設定を既定値に戻しましたと出た",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "意図した動作です。古いバージョンから更新した際、待機時間の仕組みが変わったため一度だけ既定値へ戻しています。"
      }
    },
    {
      "@type": "Question",
      "name": "変換候補が出ない／ローマ字のまま残る",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "削除と再入力は動いているものの、IME が受け取っていない状態です。"
      }
    },
    {
      "@type": "Question",
      "name": "クリップボードを操作できません",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "クリップボードを他のプロセスがロックしています。よくある原因は次のとおりです。"
      }
    },
    {
      "@type": "Question",
      "name": "キーを押しても何も起きない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "起動用のキーは設定画面で変換キーまたは右Ctrl キーから選べます。ログに trigger: Convert（または trigger: RightCtrl）が出ない場合、原因は次の 8 つです。"
      }
    },
    {
      "@type": "Question",
      "name": "救済対象の半角英数が見つかりませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "救済対象はカーソル直前の連続した半角 ASCII 印字文字です。次の場合は対象がありません。"
      }
    },
    {
      "@type": "Question",
      "name": "Enter で改行した前の行が変換されない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Enter キーで改行した位置を越えて変換することはできません。 変換されるのは、直前の改行からカーソルまでの範囲だけです（取得範囲の設定を変えても解決しません）。"
      }
    },
    {
      "@type": "Question",
      "name": "長い文章を一度に変換すると、先頭が勝手に確定される",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "一度に変換する文字数が多いと、IME が先頭のほうから順に変換を自動で確定していきます。 確定された部分は変換候補を選び直せなくなるため、誤変換の修正に手間がかかります。"
      }
    },
    {
      "@type": "Question",
      "name": "残したかった英単語まで変換されてしまう",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "apiwotukau と小文字で打つと、api の部分まで巻き込まれてアピを使うになります。小文字の英単語はローマ字と区別する方法がないためです。"
      }
    },
    {
      "@type": "Question",
      "name": "クリップボードの内容が消える／戻らない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "処理中に内部で Ctrl+C を使うため、クリップボードは必ず一度上書きされます。処理後の扱いは設定で決まります。"
      }
    },
    {
      "@type": "Question",
      "name": "処理が中断されました",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "処理中に別のウィンドウへ切り替わったか、キーボード保護の上限を超えた後にキーを押したため、途中で安全に打ち切りました。無関係なウィンドウへ文字が送り込まれるのを防ぐための動作です。文字が消えているかどうかは、通知の2行目で分かります。"
      }
    },
    {
      "@type": "Question",
      "name": "対象ウィンドウを前面にできませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "対象アプリが管理者権限で動いています。"
      }
    },
    {
      "@type": "Question",
      "name": "コピー結果がテキストではありません",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "選択範囲に画像などテキスト以外が含まれているか、対象アプリが独自形式でクリップボードに書き込んでいます。そのアプリでは動作しません。"
      }
    },
    {
      "@type": "Question",
      "name": "救済できませんでした",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "想定していない失敗です。他のメッセージに当てはまらなかった場合に表示されます。"
      }
    },
    {
      "@type": "Question",
      "name": "救済処理中に一瞬キーボードが効かなくなる",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "仕様です。処理中は物理キーの入力を握り潰しています。ユーザーの Ctrl や Shift が合成 Backspace と組み合わさり、Ctrl+Backspace（単語削除）などに化けるのを防ぐためです。"
      }
    },
    {
      "@type": "Question",
      "name": "設定が反映されない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "設定ファイルは %LOCALAPPDATA%\\ro-maji_ime_rescue\\ro-maji_ime_rescue.ini です 有効範囲外の値は読み込み時・保存時の両方で自動的に丸められます（例: 操作間待機の最小値は 20ms） 設定画面で OK を押した時点で反映されます。アプリの再起動は不要です"
      }
    },
    {
      "@type": "Question",
      "name": "自動起動を有効にできない",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "管理者権限は関係ありません。"
      }
    },
    {
      "@type": "Question",
      "name": "アンチウイルス／EDR が警告を出す",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "システム全体を監視する低レベルキーボードフック（WH_KEYBOARD_LL）を使っているため、キーロガーと同じ API として検知されることがあります。"
      }
    },
    {
      "@type": "Question",
      "name": "すでに起動していますと出る",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "多重起動を防いでいます。タスクトレイに既にアイコンがあるはずです。見当たらない場合はタスクマネージャーで ro-maji_ime_rescue.exe を終了してから起動し直してください。"
      }
    }
  ]
}
</script>
