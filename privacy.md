---
title: プライバシーポリシー
description: ro-maji IME Rescue のプライバシーポリシー。個人情報を収集・保存・送信しません。
updated: 2026-08-11
---
最終更新日: 2026-08-11

ro-maji IME Rescue（以下「本アプリ」）は、個人情報を収集・保存・送信しません。ネットワーク通信を一切行わず、広告・解析・トラッキングやアカウント機能もありません。本アプリをインストール・利用することで、本ポリシーに同意したものとみなされます。

## 本アプリが行うこと

IME を OFF のまま入力してしまった半角ローマ字を、変換キーひとつ（設定で右Ctrl キーにも変更できます）で日本語に入力し直す常駐ツールです。

## 読み取る内容

救済処理のため、カーソル直前のテキストを読み取ります（Ctrl+C を使うので、その内容はクリップボードに入ります）。読み取った内容を**本アプリのファイルに保存することはなく、送信も一切しません。** ただしクリップボードに入った内容は、設定によっては処理後も残ります（下の「クリップボードの扱い」を参照してください）。

読み取る範囲は設定で選べます。**既定は「文書先頭まで」で、カーソルより前の文書全体が読み取りの対象になります。** 読み取る量を減らしたい場合は、設定画面で「直前2行まで」または「行頭まで」に変更してください。なお、どの範囲を選んでも実際に変換される部分は変わりません（カーソル直前から続く半角の英数字・記号だけが対象です）。

**パスワード欄など、内容を知られたくない入力欄では使用しないでください。** 読み取った内容はクリップボードを経由するため、設定によっては処理後も残り、Windows のクリップボード履歴に記録されることがあります。

## クリップボードの扱い

救済処理では内部で Ctrl+C を使うため、**処理中にクリップボードを必ず一度上書きします。** 処理後の扱いは設定で選べます。既定は「そのまま保持」です。

| 設定 | 処理後のクリップボード |
|---|---|
| そのまま保持する（既定）| **読み取ったローマ字が残ります**（次に何かをコピーするまで）|
| 元の内容を復元する | 処理前の内容に戻ります（テキスト・HTML・画像・ファイル一覧など主要な形式が対象）|
| 空にする | 空になります |

「そのまま保持」でローマ字を残すのは意図的なものです。誤変換したときや処理が中断されたときに、`Ctrl+V` で元のローマ字を貼り戻せるようにするための唯一の手段になっています。「そのまま保持」または「空にする」を選んでいる場合、処理前にコピーしていた内容は失われます。

**Windows のクリップボード履歴について。** クリップボードは本アプリの領域ではなく、OS が管理する共有領域です。Windows の「クリップボードの履歴」（Win+V）を有効にしている場合、本アプリが置いた内容も履歴に記録されます。さらに「デバイス間で同期」を有効にしている場合は、Windows の機能によって他のデバイスへ送られることがあります。**これは Windows 側の機能であり、本アプリから制御できません**（本アプリ自身がネットワーク通信を行うことはありません）。内容を残したくない場合は、クリップボードの扱いを「元の内容を復元」または「空にする」に変更するか、Windows の設定でクリップボードの履歴と同期をオフにしてください。

## ローカルに保存する情報

`%LOCALAPPDATA%\ro-maji_ime_rescue\` に、設定値（ini）とデバッグログ（既定オフ）を保存します。デバッグログは有効にした場合のみ生成され、処理結果・失敗理由・文字数などの技術情報だけを記録します。入力内容やクリップボードの中身は記録しません。レジストリへの書き込みは行いません。

## キーボード監視について

起動用のキー（変換キーまたは右Ctrl キー）の単体押下を検知するため、システム全体のキーボードフック（`WH_KEYBOARD_LL`）を使います。仕組み上、すべてのキーのイベントがフックに届きます。他のキーを見るのは次の3つの目的に限られます。

- 起動用のキーが他のキーと組み合わせて押されたか（`Ctrl+C` など）を判別するため
- 救済処理の最中に打った文字が変換中の文章へ混ざらないよう、その間のキー入力をいったん止めるため
- 上記の一時停止には上限があり（既定 30 秒、設定で変更できます）、それを超えた後にキーが押されたことを検知して処理を中止するため

**キーの内容を記録することも、送信することも一切ありません。** より制限された `RegisterHotKey` では「IME が ON のときは素通しする」動作を実現できないため、こちらを使っています（セキュリティソフトがキーロガーと誤検知することがあります）。

救済処理の最中は、マウスのボタンが押されているかどうかも確認します。押されたら処理を中止するためで、ボタンの状態を問い合わせるだけです（マウス用のフックは使いません）。カーソルの位置や移動、クリックの内容は取得も記録もしません。

## その他

- 自動起動は Windows 標準の仕組み（MSIX の startupTask）を使い、既定はオフです。
- 管理者権限は不要です。常に一般ユーザー権限で動作します。
- 情報を第三者へ提供・共有することはありません。
- 本ポリシーは予告なく変更される場合があります。重要な変更は配布ページ等で告知します。

## お問い合わせ

- 開発者連絡先: moappsupport@gmail.com

---

[トップページへ戻る](./)
