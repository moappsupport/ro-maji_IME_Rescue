---
title: プライバシーポリシー
description: ro-maji IME Rescue のプライバシーポリシー。個人情報を収集・保存・送信しません。
updated: 2026-08-09
---
最終更新日: 2026-08-08

ro-maji IME Rescue（以下「本アプリ」）は、個人情報を収集・保存・送信しません。ネットワーク通信を一切行わず、広告・解析・トラッキングやアカウント機能もありません。本アプリをインストール・利用することで、本ポリシーに同意したものとみなされます。

## 本アプリが行うこと

IME を OFF のまま入力してしまった半角ローマ字を、変換キーひとつで日本語に入力し直す常駐ツールです。

## 読み取る内容

救済処理のため、カーソル直前のテキストを一時的に読み取ります（Ctrl+C を使うので、その内容は一時的にクリップボードに入ります）。読み取った内容は変換処理にのみ使い、保存も送信もしません。

読み取る範囲は設定で選べます（行頭まで／直前2行まで／文書先頭まで）。「文書先頭まで」を選ぶと、カーソルより前の文書全体が対象になります。

パスワード欄など、内容を知られたくない入力欄では使用しないでください。

## クリップボードの扱い

処理中は一時的に上書きします。処理後の扱いは設定で選べ（そのまま保持／元の内容を復元／空にする）、既定は「そのまま保持」です。「そのまま保持」または「空にする」を選んでいる場合、処理前にコピーしていた内容は失われます。「元の内容を復元」を選んでいる場合は、テキスト・HTML・画像・ファイル一覧など主要な形式であれば元に戻ります。

## ローカルに保存する情報

`%LOCALAPPDATA%\ro-maji_ime_rescue\` に、設定値（ini）とデバッグログ（既定オフ）を保存します。デバッグログは有効にした場合のみ生成され、処理結果・失敗理由・文字数などの技術情報だけを記録します。入力内容やクリップボードの中身は記録しません。レジストリへの書き込みは行いません。

## キーボード監視について

変換キー単体の押下を検知するため、システム全体のキーボードフック（`WH_KEYBOARD_LL`）を使います。見ているのは変換キーの押下・離上のタイミングだけで、他のキー入力は記録も送信もしません。より制限された `RegisterHotKey` では「IME が ON のときは素通しする」動作を実現できないため、こちらを使っています（セキュリティソフトがキーロガーと誤検知することがあります）。

## その他

- 自動起動は Windows 標準の仕組み（MSIX の startupTask）を使い、既定はオフです。
- 管理者権限は不要です。常に一般ユーザー権限で動作します。
- 情報を第三者へ提供・共有することはありません。
- 本ポリシーは予告なく変更される場合があります。重要な変更は配布ページ等で告知します。

## お問い合わせ

- 開発者連絡先: moappsupport@gmail.com

---

[トップページへ戻る](./)
