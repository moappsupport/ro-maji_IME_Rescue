---
title: プライバシーポリシー
---

# プライバシーポリシー（ro-maji IME Rescue）

最終更新日: 2026-07-27

ro-maji IME Rescue（以下「本アプリ」）のプライバシーポリシーです。本アプリをインストール・利用することで、本ポリシーに同意したものとみなされます。

## 本アプリが行うこと

本アプリは、IME を OFF にしたまま入力してしまった半角ローマ字を、変換キーを押すことで日本語に入力し直す常駐ツールです。

## 収集する個人情報

**本アプリは、個人情報を収集・保存・送信しません。**

- **ネットワーク通信を一切行いません。** サーバーへの送信機能を持たず、パッケージのマニフェストでもネットワーク関連の権限を宣言していません。
- 入力されたキーストロークの内容、クリップボードの中身、ユーザーが作成・編集した文書の内容を外部に送信することはありません。
- アカウント登録やログイン機能はありません。
- 広告・解析・トラッキングの仕組みを一切含みません。

## 本アプリが読み取る内容

本アプリは救済処理を行うため、**カーソル直前のテキストを一時的に読み取ります**。これは本アプリの動作に必要な処理で、読み取った内容はその場の変換処理にのみ使われ、保存も送信もされません。

読み取る範囲は設定で選択できます。

| 設定 | 読み取る範囲 |
|---|---|
| 行頭まで（既定） | 現在行の先頭からカーソル位置まで |
| 直前2行まで | 1つ上の行の先頭からカーソル位置まで |
| 文書先頭まで | 文書の先頭からカーソル位置まで |

「文書先頭まで」を選んだ場合、カーソルより前の文書全体が一時的に読み取り対象になります。読み取りには Ctrl+C（コピー）を使うため、その内容は一時的にクリップボードへ入ります。

パスワード欄など、内容を知られたくない入力欄では本アプリを使用しないでください。

## ローカルに保存する情報

本アプリの動作のため、以下の情報をユーザーの PC 内（`%LOCALAPPDATA%\ro-maji_ime_rescue\`）にのみ保存します。外部への送信は行いません。

| 保存内容 | ファイル | 内容 |
|---|---|---|
| 設定値 | `ro-maji_ime_rescue.ini` | 待機時間、取得範囲、クリップボードの扱いなどの設定値のみ。個人を特定する情報は含みません。 |
| デバッグログ（任意・既定オフ） | `ro-maji_ime_rescue_debug.txt` | ユーザーが明示的にデバッグモードを有効にした場合のみ生成されます。処理結果・失敗理由・設定値・文字数などの技術的情報のみを記録し、**入力内容やクリップボードの中身そのものは一切記録しません**。 |

デバッグログはユーザーが自由に削除できます。トレイメニューから保存フォルダーを開けます。

**レジストリへの書き込みは行いません。**

## キーボード監視について

本アプリは、変換キー単体の押下を検知するためにシステム全体のキーボードフック（Windows の `WH_KEYBOARD_LL`）を使用します。

このフックが行っているのは**変換キーの押下・離上のタイミングを見ることだけ**です。それ以外のキー入力の内容を記録・保存・送信することはありません。

より制限された API（`RegisterHotKey`）を使わないのは、「IME が ON のときは通常の変換キーとして素通しする」という動作が実現できないためです。この仕組みは一般的なキーロガーが使う API と同じ種類のため、セキュリティソフトが誤検知して警告を出すことがあります。

## クリップボードについて

救済処理中、対象の文字列を取得するために**クリップボードを一時的に上書きします**。処理後の扱いは設定で選択できます。

| 設定 | 処理後の状態 |
|---|---|
| そのまま保持する（既定） | 処理でコピーした文字列がクリップボードに残ります |
| 元の内容を復元する | 処理前のテキストが復元されます（画像やファイルなど非テキスト形式は復元されず、空になります） |
| 空にする | クリップボードが空になります |

**既定では、処理でコピーした文字列がクリップボードに残ります。** 処理前にコピーしていた内容は失われます。クリップボードの内容が外部に送信されることはありません。

## 自動起動について

設定画面から Windows 起動時の自動起動を有効にできます（既定はオフ）。これは Windows の標準的な自動起動の仕組み（MSIX の startupTask）を使ったもので、有効・無効の状態は Windows が管理します。タスク マネージャーの「スタートアップ アプリ」からも切り替えられます。

## 必要な権限

本アプリは**管理者権限を必要としません**。常に一般ユーザー権限で動作します。

## 第三者への提供

本アプリは、いかなる情報も第三者へ提供・共有しません。

## 本ポリシーの変更

本ポリシーは予告なく変更される場合があります。重要な変更がある場合は、本アプリの配布ページ等で告知します。

## お問い合わせ

本アプリまたは本ポリシーに関するお問い合わせは、以下までご連絡ください。

- 開発者連絡先: moappsupport@gmail.com

---

[トップページへ戻る](./)
